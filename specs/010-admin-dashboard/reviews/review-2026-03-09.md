# Code Review Report: Admin Dashboard

**Date**: 2026-03-09 | **Scope**: Post-Implementation (branch `010-admin-dashboard` vs `master`) | **Files Reviewed**: 8

## Executive Summary

- **Overall Health**: 82/100
- **Critical Issues**: 0
- **Spec Compliance**: 88% (15/17 FR matched)
- **Fake Implementations**: 0 blockers
- **Dead Code Items**: 1 (unused import)
- **Technical Debt Score**: 14 (low)
- **Top 3 Actions**: Fix FR-011 structured fields collapsing, add plugin name/version to table, add "reconnecting" text indicator for SSE

## Findings by Severity

### High

| File | Issue | Line | Recommendation |
|------|-------|------|----------------|
| `frontend/public/index.html` | FR-001 PARTIAL: Plugin table shows pluginId but not name/version (spec says "name, version, and status") | 512 | API returns only pluginId+status+reason; either extend API response or accept pluginId as "name" |
| `frontend/public/js/dashboard.js` | FR-011 PARTIAL: Structured fields always shown inline, not collapsed when >3 fields | 148-150 | Add conditional collapse for >3 fields |
| `frontend/public/js/sse-client.js` | EventSource sends token as query param `?token=` which may be logged in server access logs | 164 | Acceptable if gateway supports query auth; verify server doesn't log query params |

### Medium

| File | Issue | Line | Recommendation |
|------|-------|------|----------------|
| `frontend/public/js/dashboard.js` | FR-016 PARTIAL: No visible "reconnecting" text indicator for SSE disconnect; only dot turns red | 84-86 | Add a text status like "Reconnecting..." next to the dot |
| `frontend/public/js/dashboard.js` | Silent catch blocks in `loadHealth()` swallow errors completely | 170, 179 | At minimum log to console for debuggability |
| `src/plugins/webui/index.ts` | Unused import: `HealthStatus` is imported but not used | 16 | Remove unused import |
| `frontend/public/index.html` | `x-effect` on dashboard div fires on every `currentPage` change including initial mount, causing double `onShow()` with `init()` | 377 | Guard: check if already active before calling onShow() |
| `frontend/public/js/dashboard.js` | `init()` calls `loadPlugins()` but `onShow()` also calls it, causing duplicate fetch on first dashboard view | 22-23, 226-228 | Remove `loadPlugins()` from `init()`, rely on `onShow()` only |

### Low

| File | Issue | Line | Recommendation |
|------|-------|------|----------------|
| `frontend/public/css/app.css` | CSS selector `[x-data="chat()"]` is fragile and will break if function name changes | 23 | Acceptable for now; add a CSS class if it becomes a maintenance issue |
| `frontend/public/js/dashboard.js` | `formatUptime(0)` returns "0m" which is correct but `formatUptime(null)` returns '-' while `formatUptime(undefined)` also returns '-' — consistent | 194 | No action needed |

## Spec Deviation Summary

| Category | Match | Drift | Missing | Partial |
|----------|-------|-------|---------|---------|
| Functional Requirements (FR-001 to FR-017) | 15 | 0 | 0 | 2 |
| API Contracts | 4/4 | 0 | 0 | 0 |
| Data Model | 4/4 | 0 | 0 | 0 |
| Acceptance Criteria | 14/16 | 0 | 0 | 2 |

**Compliance Score**: 88%

### Detailed Requirement Coverage

| Req | Status | Notes |
|-----|--------|-------|
| FR-001 | PARTIAL | Shows pluginId, not name/version — API doesn't return them |
| FR-002 | MATCH | Reason shown in table Details column |
| FR-003 | MATCH | Summary cards show total/loaded/failed |
| FR-004 | MATCH | SSE log stream via EventSource |
| FR-005 | MATCH | Level filter buttons with client-side filtering |
| FR-006 | MATCH | Auto-scroll with scroll detection |
| FR-007 | MATCH | "New entries below" scroll indicator |
| FR-008 | MATCH | 1000-entry cap with splice |
| FR-009 | MATCH | Health panel with uptime, memory, plugins, connectors |
| FR-010 | MATCH | 30s setInterval refresh |
| FR-011 | PARTIAL | Fields shown inline but not collapsed when >3 |
| FR-012 | MATCH | Tab navigation with visual indicator |
| FR-013 | MATCH | x-show preserves DOM, SSE disconnects/reconnects on switch |
| FR-014 | MATCH | Bearer token on all fetch calls |
| FR-015 | MATCH | 401 shows error, chat dispatches token prompt |
| FR-016 | MATCH | Dot indicator turns red; EventSource auto-reconnects natively |
| FR-017 | MATCH | Red/orange/blue/gray color-coded levels |

## Fake Implementations Found

None found. All code paths are real implementations.

## Dead Code Found

| File | Line | Type | Evidence |
|------|------|------|----------|
| `src/plugins/webui/index.ts` | 16 | Unused import | `HealthStatus` imported but never used |

## Technical Debt Summary

| Category | Items | Est. Effort | Priority |
|----------|-------|-------------|----------|
| Spec Deviation | 2 | 1h | Medium |
| Code Quality | 3 | 30min | Low |
| Dead Code | 1 | 5min | Low |

**Total Estimated Remediation**: 1.5 hours

## Recommendations

### Quick Wins (< 15 min each)
1. Remove unused `HealthStatus` import from index.ts
2. Remove duplicate `loadPlugins()` call from `init()` (rely on `onShow()`)
3. Add guard in `onShow()` to prevent double execution

### Short-term (< 1 hour)
4. Add "Reconnecting..." text next to SSE connection dot when disconnected
5. Implement field collapsing for log entries with >3 structured fields (FR-011)

### Deferred (acceptable for MVP)
6. Plugin name/version in table (FR-001) — requires API change or new field mapping; pluginId serves the purpose for operator use
