# Code Review Report

**Date**: 2026-03-17 | **Scope**: Post-Implementation (012-memory-dashboard) | **Files Reviewed**: 11

## Executive Summary

- **Overall Health**: 72/100
- **Critical Issues**: 0
- **High Issues**: 3 (S-1 double decodeURIComponent, D-1 edges empty, F-1 edgeCount fallback)
- **Spec Compliance**: 85% (8.5/10 endpoints fully compliant)
- **Fake Implementations**: 2 (F-1 edgeCount stub, F-2 timeline scan cap)
- **Dead Code Items**: 2 (DC-1 unused scroll vars, DC-2 unused _ctx param)
- **Technical Debt Score**: 34 (manageable)
- **Top 3 Actions**: Fix S-1 double-decode, extract AssociationGraphLike, extract readBody

## Findings by Severity

### High

| ID | File | Issue | Recommendation |
|----|------|-------|----------------|
| S-1 | memory-files.ts:25 | Double `decodeURIComponent` enables path traversal bypass | Remove `decodeURIComponent` inside `validatePath` |
| D-1 | memory-graph.ts:41 | Edges empty when graph internal storage is not a `Map` | Use graph service method instead of internal property |
| F-1 | memory-stats.ts:43 | `edgeCount` fallback is `0`, misleading comment | Same as D-1 — use service method |

### Medium

| ID | File | Issue | Recommendation |
|----|------|-------|----------------|
| C-1 | 3 handler files | `AssociationGraphLike` triplicated with divergent shapes | Extract to shared types |
| C-2 | memory-files.ts, memory-notes.ts | `readBody` duplicated | Extract to shared utility |
| D-2 | memory-search.ts:62 | `GraphEdge.created` returns empty string | Acceptable: neighbors() doesn't return created |
| D-3 | memory-timeline.ts:56 | `offset` skips populated days, not calendar days | Document actual semantics |
| F-2 | memory-timeline.ts:40 | Timeline scan cap can silently return fewer results | Increase scan multiplier |
| P-2 | memory-files.ts:37 | `buildTree` has no depth limit | Add depth cap (5 levels) |

### Low

| ID | File | Issue | Recommendation |
|----|------|-------|----------------|
| DC-1 | memory.js:118-119 | `_savedExplorerScroll`, `_savedTimelineScroll` unused | Remove |
| C-3 | memory.js:249 | `initGraph()` is 143 lines | Acceptable for CDN-only frontend |
| C-5 | memory.js:275 | Edge ID collision risk with hyphens | Use `\|` delimiter |
| C-6 | memory.js:125 | Redundant `loadStats()` on tab switch | Remove from switchTab |
| M-1 | memory-files.ts:122 | `lastIndexOf('/')` instead of `path.dirname` | Use `path.dirname()` |

## Spec Deviation Summary

| Category | Match | Drift | Missing | Partial |
|----------|-------|-------|---------|---------|
| API Contracts (10 endpoints) | 7 | 2 (D-1, D-3) | 0 | 1 (D-2) |
| Data Model | 8 | 1 (created field) | 0 | 0 |

**Compliance Score**: 85%

## Technical Debt Summary

| Category | Items | Est. Effort | Priority |
|----------|-------|-------------|----------|
| Security | 1 (S-1) | 10 min | P0 |
| Code Debt | 3 (C-1, C-2, M-1) | 30 min | P1 |
| Spec Deviation | 3 (D-1, D-2, D-3) | 20 min | P1 |
| Dead Code | 2 (DC-1, DC-2) | 5 min | P2 |
| Performance | 2 (P-2, F-2) | 15 min | P2 |

**Total Estimated Remediation**: ~1.5 hours

## Recommendations

### Quick Wins (< 15 min)
- Fix S-1: Remove duplicate `decodeURIComponent` in `validatePath`
- Fix DC-1: Remove unused `_savedExplorerScroll` and `_savedTimelineScroll`
- Fix M-1: Replace `lastIndexOf('/')` with `path.dirname()`
- Fix C-6: Remove redundant `loadStats()` from `switchTab()`
- Fix C-5: Use `|` delimiter for Cytoscape edge IDs

### Short-term (< 1 hour)
- Fix C-1: Extract `AssociationGraphLike` to shared types
- Fix C-2: Extract `readBody` to shared utility
- Fix P-2: Add depth limit to `buildTree`
- Fix F-2: Increase timeline scan multiplier to 30

### Long-term (requires planning)
- D-1: Add `edgeCount()` and `getAllEdges()` to AssociationGraph service
- S-4: Add SRI hashes to all CDN resources
