# Code Review Report

**Date**: 2026-03-09 | **Scope**: Post-Implementation (007-association-graph) | **Files Reviewed**: 4

## Executive Summary

- **Overall Health**: 82/100
- **Critical Issues**: 0
- **Spec Compliance**: 94% (15/16 requirements matched)
- **Fake Implementations**: 0 blockers
- **Dead Code Items**: 1 (unused import)
- **Technical Debt Score**: 12

## Findings by Severity

### High

| File | Issue | Line | Recommendation |
|------|-------|------|----------------|
| association-graph.ts | `as any` casts in EventBus publish/subscribe bypass type safety | memory-store.ts:354, index.ts:57 | Extend EventMap via declaration merging for `memory:upserted` |
| association-graph.ts | No 5K node warning logged (spec: "Warning logged at 5,000 nodes") | — | Add nodeCount check in addNode() |
| association-graph.test.ts | Missing tests for `extractAndMerge()` (mock LLM adapter) | — | Add extraction tests with mock adapter |

### Medium

| File | Issue | Line | Recommendation |
|------|-------|------|----------------|
| association-graph.ts | Dijkstra uses linear scan O(V²) — acceptable for <10K but not ideal | 219-229 | OK for MVP, consider priority queue for scaling |
| association-graph.test.ts | Unused import `GraphEdge` | 2 | Remove unused import |
| memory/index.ts | `graph.load().catch(() => {})` swallows errors silently in setup | 46 | Log error via context.logger |

### Low

| File | Issue | Line | Recommendation |
|------|-------|------|----------------|
| memory-store.ts | `dirty` field in AssociationGraph is set but never read externally | 52 | Acceptable — internal bookkeeping |

## Spec Deviation Summary

| Category | Match | Drift | Missing | Partial |
|----------|-------|-------|---------|---------|
| Functional Requirements (FR-001 to FR-016) | 14 | 0 | 1 | 1 |

**Compliance Score**: 94%

### Missing/Partial

| Requirement | Expected | Actual | Status |
|-------------|----------|--------|--------|
| FR-004 | Base relation vocabulary exported and documented | `BASE_RELATIONS` exported but not used for validation/normalization | PARTIAL — constant exists but relations are free-form (matches spec "while accepting normalized free-form relations") — actually MATCH on re-read |
| Spec: 5K warning | Warning logged at 5,000 nodes | No warning implemented | MISSING |
| FR-016 | Context injection ~200 token limit | 10 concept lines cap but no explicit token counting | PARTIAL — line cap approximates 200 tokens |

## Fake Implementations Found

None — all methods are fully implemented.

## Dead Code Found

| File | Line | Type | Evidence |
|------|------|------|----------|
| association-graph.test.ts | 2 | Unused import | `GraphEdge` imported but never used |

## Technical Debt Summary

| Category | Items | Est. Effort | Priority |
|----------|-------|-------------|----------|
| Type Safety | 2 (`as any` casts) | 30min | P2 |
| Test Coverage | 1 (extraction tests missing) | 1hr | P1 |
| Spec Compliance | 1 (5K warning) | 15min | P3 |
| Error Handling | 1 (swallowed load error) | 10min | P3 |

**Total Estimated Remediation**: 2 hours

## Recommendations

### Quick Wins (< 30 min each)
1. Remove unused `GraphEdge` import from test file
2. Add `memory:upserted` event to EventMap via declaration merging (eliminates `as any`)
3. Log graph load errors via context.logger instead of swallowing
4. Add 5K node warning in `addNode()`

### Short-term (1-2 hours)
1. Add extraction tests with mock LLM adapter (verify merge, error handling, null adapter)
