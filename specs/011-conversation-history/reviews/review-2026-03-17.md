# Code Review Report

**Date**: 2026-03-17 | **Scope**: Post-Implementation (011-conversation-history) | **Files Reviewed**: 12

## Executive Summary

- **Overall Health**: 82/100
- **Critical Issues**: 0
- **Spec Compliance**: 93% (14/15 items)
- **Fake Implementations**: 1 (swallowed catch, non-critical)
- **Dead Code Items**: 2
- **Technical Debt Score**: 12
- **Top 3 Actions**: Fix duplicate "System Info" comment, add error handler wrapping to CRUD handlers, remove unused `randomUUID` import

## Findings by Severity

### High

| File | Issue | Line | Recommendation |
|------|-------|------|----------------|
| `handlers/chat.ts` | Unused `randomUUID` import | 10 | Remove — `crypto.randomUUID()` is used in ConversationStore, not here anymore |
| `handlers/chat.ts` | Title generation runs after SSE headers sent but before `res.end()` — if title gen completes after `res.end()` in finally block, `writeEvent` will silently fail | 215-222 | The `res.writableEnded` guard handles this, but the timing race is subtle. Add a comment explaining the design. |
| `handlers/conversations.ts` | No try-catch around store operations — unhandled errors crash the handler | 23, 46, 84 | Wrap store calls in try-catch, return 500 on unexpected errors |

### Medium

| File | Issue | Line | Recommendation |
|------|-------|------|----------------|
| `types.ts` | Duplicate "System Info" section comment | 74-75, 105-107 | Remove the empty duplicate at line 74-75 |
| `types.ts` | `ConversationMessageSchema.msg` uses loose `z.record(z.string(), z.unknown())` vs spec's discriminated union | 94 | Acceptable deviation — the loose schema is more resilient to format changes, but diverges from the contract spec. Documented as deliberate. |
| `chat.ts` | `noTools` and `maxTokens` and `maxSteps` may not exist on `LlmCompletionInput` type | 206-208 | Verify these fields compile — they may be ignored silently |
| `conversations.js` | `handleKeydown` fires on `@keydown.window` — captures all keystrokes globally | 87 | Only handle when sidebar has focus. Add a guard: `if (!document.activeElement?.closest('aside'))` |

### Low

| File | Issue | Line | Recommendation |
|------|-------|------|----------------|
| `conversation-store.ts` | `console.warn` for logging instead of structured logger | 212, 215 | Acceptable for now — store has no DI access to StructuredLogger |
| `chat.js` | `loadConversation` is 75 lines — moderately long method | 168-243 | Could extract message mapping to a helper, but acceptable for now |

## Spec Deviation Summary

| Category | Match | Drift | Missing | Partial |
|----------|-------|-------|---------|---------|
| API Contracts | 4 | 0 | 0 | 0 |
| Data Model | 3 | 1 | 0 | 0 |
| SSE Events | 2 | 0 | 0 | 0 |
| Frontend | 4 | 0 | 0 | 1 |

**Compliance Score**: 93% (14/15)

**Details**:
- **DRIFT**: `ConversationMessageSchema.msg` uses `z.record()` instead of spec's discriminated union — deliberate for resilience
- **PARTIAL**: Sidebar `@keydown.window` captures globally instead of scoped to sidebar focus

## Fake Implementations Found

| File | Line | Type | Snippet | Severity |
|------|------|------|---------|----------|
| `chat.ts` | 222 | Swallowed error | `.catch(() => { /* title generation failure is non-critical */ })` | Low (documented, intentional) |
| `chat.ts` | 228 | Swallowed error | `.catch(() => {})` on `updatePreview` | Medium — add at least `logger.warn` |

## Dead Code Found

| File | Line | Type | Evidence |
|------|------|------|----------|
| `chat.ts` | 10 | Unused import | `randomUUID` from `node:crypto` — no longer used after sessions Map removal |
| `types.ts` | 74-75 | Duplicate comment | "System Info (RPC response)" section header appears twice |

## Technical Debt Summary

| Category | Items | Est. Effort | Priority |
|----------|-------|-------------|----------|
| Dead Code | 2 | 5 min | Quick win |
| Error Handling | 2 | 30 min | Short-term |
| Keyboard a11y | 1 | 15 min | Short-term |

**Total Estimated Remediation**: 50 minutes

## Recommendations

### Quick Wins (< 15 min)
1. Remove unused `randomUUID` import from `chat.ts`
2. Remove duplicate "System Info" comment block from `types.ts`
3. Add logger.warn to `updatePreview` catch block

### Short-term (15-60 min)
4. Add try-catch error handling to conversation CRUD handlers
5. Scope keyboard navigation to sidebar focus only
