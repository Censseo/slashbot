# Code Review Report

**Date**: 2026-02-19 | **Scope**: Post-Implementation (branch `003-nodered-setup-skill` vs `master`) | **Files Reviewed**: 6 source files, 4 test files, 1 skill file

## Executive Summary

- **Overall Health**: 72/100
- **Critical Issues**: 1 (53 failing legacy tests)
- **Spec Compliance**: 94% (16/17 FRs matched, 1 pending validation)
- **Fake Implementations**: 0 blockers
- **Dead Code Items**: 1 (stale test file)
- **Technical Debt Score**: 18

## Findings by Severity

### Critical

| File | Issue | Line | Recommendation |
|------|-------|------|----------------|
| `src/plugins/nodered/services/NodeRedManager.test.ts` | 53 of 106 tests failing — stale tests reference removed `ensureNodeRedInstalled()` and npm install logic | Multiple | Update or remove stale tests to match refactored NodeRedManager |

### High

| File | Issue | Line | Recommendation |
|------|-------|------|----------------|
| `src/plugins/nodered/services/NodeRedManager.test.ts` | Tests reference deleted npm install behavior (FR-012 violation in test expectations) | 508-600 | Remove tests for `ensureNodeRedInstalled()`, update state transition tests for new `setup-needed` flow |
| `src/plugins/nodered/services/NodeRedManager.ts` | `emitNodeRedEvent` uses `as any` cast for EventBus publish | 108 | Minor type safety concern; acceptable for internal event emission |

### Medium

| File | Issue | Line | Recommendation |
|------|-------|------|----------------|
| `src/plugins/nodered/index.ts` | `OnceJobScheduler` interface declared inline inside setup() | 478-480 | Extract to types.ts or a contracts file for reuse |
| `src/plugins/nodered/services/NodeRedManager.ts` | `logFileHandle` opened async with fire-and-forget `.then()` — potential race if log lines arrive before handle is ready | 493-497 | Acceptable; fallback path exists at line 536 |
| `src/plugins/nodered/services/NodeRedManager.ts` | `response.ok && response.status === 200` is redundant — `ok` already means 200-299 | 190, 319, 452 | Simplify to `response.ok` only (cosmetic) |

### Low

| File | Issue | Line | Recommendation |
|------|-------|------|----------------|
| `src/plugins/nodered/services/NodeRedManager.ts` | Magic number `200` for RingBuffer capacity | 70 | Extract to named constant |
| `src/plugins/nodered/services/NodeRedManager.ts` | Magic number `2000` for AbortSignal timeout | 187, 316, 383, 449 | Extract to named constant |
| `src/plugins/nodered/prompt.ts` | References `Alias: /nr` but no alias registration found in plugin | 18 | Verify alias exists or remove from prompt |

## Spec Deviation Summary

| Category | Match | Drift | Missing | Partial |
|----------|-------|-------|---------|---------|
| Functional Requirements (FR-001–FR-018) | 16 | 0 | 0 | 1 |
| Acceptance Criteria | 12 | 0 | 2 | 2 |

**Compliance Score**: 94%

### Partial Requirements

| Requirement | Expected | Actual | Status |
|-------------|----------|--------|--------|
| FR-010 (crash → skill restart) | Crash triggers one-shot automation job to invoke skill | Crash triggers automation job but message says "run nodered-setup skill" — relies on bot interpreting correctly | MATCH (acceptable) |
| T023 (quickstart validation) | End-to-end validation | Not yet executed | PENDING |

### Missing Acceptance Criteria Verification

- US2-AC4: "Given the heartbeat detects Node-RED has crashed, When the crash is detected, Then the bot autonomously invokes the skill to restart" — code path exists but no integration test
- US3-AC3: "Given Node-RED was running, When a heartbeat fails, Then the plugin transitions to unavailable" — heartbeat failure triggers kill + crash recovery, not a direct `unavailable` transition. This is a spec drift: the spec says "unavailable" but the code correctly uses `failed` state after retries exhausted.

## Dead Code Found

| File | Line | Type | Evidence |
|------|------|------|----------|
| `src/plugins/nodered/services/NodeRedManager.test.ts` | 1-2600+ | Stale test file | 53/106 tests fail; tests reference removed `ensureNodeRedInstalled()` method and npm install flow. This is the feature 001 test file that was NOT updated during feature 003 refactoring. |

## Technical Debt Summary

| Category | Items | Priority |
|----------|-------|----------|
| Test Debt | 1 (53 failing legacy tests) | P0 — blocks CI |
| Code Debt | 3 (inline interface, redundant checks, magic numbers) | P3 |
| Spec Deviation Debt | 1 (US3-AC3 wording vs implementation) | P2 |

## Recommendations

### Quick Wins
- Remove or update stale tests in `src/plugins/nodered/services/NodeRedManager.test.ts` to match refactored NodeRedManager (this is the most impactful fix)
- Simplify `response.ok && response.status === 200` to `response.ok` in 3 locations

### Short-term
- Execute T023 (quickstart validation) to verify end-to-end flow
- Verify `/nr` alias exists or remove reference from prompt.ts

### Long-term
- Extract magic numbers (ring buffer size, health check timeout) to config constants
- Move `OnceJobScheduler` interface to a shared contracts location
