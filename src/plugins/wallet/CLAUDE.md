# Wallet Plugin Guidelines

> Auto-generated by `/specforge.learn` on 2026-02-12
> Layer: Feature Plugin | Parent: src/plugins/

## Layer Pattern

Architectural role: Solana wallet management, $SLASHBOT token economy, proxy billing, and encrypted key storage. Largest plugin (14 files, commands.ts is 49KB).

File convention: Standard plugin structure + `services/` for business logic + `provider.ts` for auth

```
src/plugins/wallet/
├── index.ts              # WalletPlugin class
├── commands.ts           # /wallet, /balance, /send, /redeem, /pricing, /usage
├── parser.ts             # XML action parsers
├── executors.ts          # Action executors
├── tools.ts              # AI SDK tool definitions
├── prompt.ts             # Prompt contribution
├── provider.ts           # ProxyAuthProvider (request-body signing)
├── types.ts              # Action types
└── services/
    ├── wallet.ts         # WalletService (lifecycle, key management)
    ├── solana.ts         # SolanaService (on-chain operations)
    ├── crypto.ts         # CryptoService (AES-256-GCM encryption)
    ├── pricingService.ts # PricingService (cost calculation)
    ├── exchangeRates.ts  # Real-time SOL/SLASHBOT price feeds
    └── xaiPricing.ts     # Model pricing table
```

## Function Signatures & Spec Sources

| Function | Signature | Purpose |
|----------|-----------|---------|
| CryptoService.encrypt | `(data: string, password: string) => EncryptedData` | AES-256-GCM encryption |
| CryptoService.decrypt | `(encrypted: EncryptedData, password: string) => string` | AES-256-GCM decryption |
| WalletService.create | `(password: string) => WalletInfo` | Generate new Solana keypair |
| WalletService.unlock | `(password: string) => void` | Decrypt and load wallet |
| SolanaService.getBalance | `(address: string) => number` | Get SOL balance |
| SolanaService.sendToken | `(to: string, amount: number) => string` | Send SLASHBOT tokens |
| ProxyAuthProvider.sign | `(body: string) => SignedRequest` | Sign request for proxy |

## Injected Dependencies

| Dependency | Source | What It Provides |
|------------|--------|------------------|
| ConfigManager | core/config | Wallet config paths, proxy URL |
| EventBus | core/events | wallet:unlocked, wallet:locked events |
| GrokClient | core/api | Auth provider injection (onAfterGrokInit) |

## Expected Errors

| Error | Handling | Context |
|-------|----------|---------|
| Wrong password | Display error, prompt retry | Wallet decrypt failure |
| Insufficient balance | Display balance + required amount | Token transfer |
| Network error (Solana) | Display with retry suggestion | RPC connection failure |
| Session expired | Prompt for password again | 30-minute timeout |

## Guard Rails

| Rule | Reason |
|------|--------|
| DO NOT log seed phrases, private keys, or passwords | Security invariant SI-2 |
| DO NOT store unencrypted wallet data | Must use AES-256-GCM (DI-1) |
| DO NOT skip session timeout check | 30-minute auth timeout (WI-3) |
| DO NOT hardcode proxy URL | Configurable via wallet-config.json |

## Token Details

| Property | Value |
|----------|-------|
| Name | SLASHBOT |
| Chain | Solana |
| Standard | SPL Token |
| Mint | AtiFyHm6UMNLXCWJGLqhxSwvr3n3MgFKxppkKWUoBAGS |
| Treasury | DVGjCZVJ3jMw8gsHAQjuYFMj8xQJyVf17qKrciYCS9u7 |
| Pricing | 2.5x xAI base rates |

<!-- MANUAL ADDITIONS START -->
<!-- MANUAL ADDITIONS END -->
